
DATABASE STRUCTURE/SETUP

DB NAME = attendance
TABLE's = professor, studaccounts, students

professor colomn's ( id, profName, profSubject, classCourseY, totalStud, timeIn ,timeOut )
id = check AI
totalStud = INT , the rest = VARCHAR

studaccounts column's (id, studentName, studentId )
id = AI, the rest VARCHAR

students column's (id, studentName, studentId, timeIn, pcNum)
id = AI, the rest VARCHAR